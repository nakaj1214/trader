# Archive

`legacy/` には、現在の JP inflection shadow scan から使われない旧週次予測、旧 GUI、Docker、設定、データ、テストを、元のディレクトリ構造に近い形で保存しています。

これらは参照専用です。通常の package、lint、test、GitHub Actions の対象には含めません。復元する場合は必要な機能だけを現行 API に移植し、archive 全体を実行経路へ戻さないでください。
