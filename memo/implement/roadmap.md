# 残りの実装ロードマップ

作成日: 2026-09-10
更新日: 2026-09-14（実装・レビュー状況をコードと突き合わせて反映）

> `memo/implement/proposal.md`のうち、まだ本番反映されていない項目の現状整理と推奨実装順。
> 各項目の詳細計画は個別ファイルを参照。本ファイルは順序と理由のみを記す。

## 現状一覧

| REQ | 内容 | 状態 | 計画ファイル |
|---|---|---|---|
| 018, 019, 021 | 新規上場銘柄の候補対象化・スコア命名明確化・市場区分別カバレッジ可視化 | **実装済み**（`review.md`でVERDICT: PASS、テスト195件・カバレッジ91.74%） | 削除済み（`plan.md`） |
| 023〜026 | Position Exit Monitor（当日High反映・stale閾値分岐・通知重複排除・Sheets原子的更新） | **実装済み**（`fetch_today_bars`／stale閾値分岐／`sheets_client.batch_update`／`cancel-in-progress: false`まで反映済み） | 削除済み（`phase_4A.md`） |
| 014 | ポートフォリオレベル評価（Portfolio Simulation） | **実装済み・自動検証PASS**（point-in-time、未決済position、cost連続性、レポート・CI・文書配線を反映。実データoperator-runのみ未実施） | `memo/implement/plan_req014.md` |
| 014b | REQ-014のセクター上限・dashboard配線（REQ-014から分離） | **未着手・分離承認済み**。業種データの取得仕様（REQ-022の未確定事項3と共通）が固まった後に計画する | なし（分離のみ。REQ-022の業種フィールド確定後に計画する） |
| 022 | モメンタム相対化（TOPIX/業種相対）のshadow A/B/C比較 | 方針未確定（着手前に決めることが7項目残っている） | `memo/implement/plan_req022.md` |
| 017 | `deep_candidates`本数比較 | 未計画・データ蓄積待ち | なし（計画不要、データが貯まってから着手） |
| 020 | スコア閾値・重みのband別検証 | 未計画・データ蓄積待ち | なし（同上） |
| 027 | 実売買履歴（Trade Ledger） | 意図的に凍結（実売買が発生するまで着手しない） | `memo/implement/plan_req027.md` |

## 推奨実装順

1. ~~REQ-018, 019, 021（`plan.md`）を仕上げて実装する~~ → **完了**。
   REQ-018でsnapshot構造が変わり`STRATEGY_VERSION`をv3・`REPORT_SCHEMA_VERSION`を4に上げたため、出荷した2026-09-10時点からforward validationのv3データ蓄積が始まっている。

   ~~REQ-023〜026（`phase_4A.md`）~~ → **完了**（roadmap初版の表には未記載だったが、REQ-018と同じコミットで実装済み）。

2. ~~REQ-014（`plan_req014.md`）の再レビュー指摘を直す~~ → **完了**。
   point-in-time・cost・未決済positionの修正、レポート・文書・CI配線、回帰テスト4b〜4eまで反映済み。セクター上限・dashboard配線はREQ-014bへ分離承認済み。

3. **REQ-022（`plan_req022.md`）の方針を確定してから実装する**
   独立3パイプライン相当の規模で、着手前に7つの未確定事項（共有/分岐境界、`STRATEGY_VERSION`命名、業種フィールド名確認等）を決める必要がある。forward validation拡張も絡むため、REQ-018出荷後のv3データがある程度貯まってから検証する方が効率的（2026-09-14時点でまだ4日分、着手にはまだ早い）。

4. **REQ-017 / REQ-020 を再計画する**
   REQ-018出荷後のv3 snapshotが数週間〜十分な日数分貯まってから着手する。今動いても判断材料が無く手戻りになるだけ。

5. **REQ-027（`plan_req027.md`）は他と独立**
   実売買を始めた時点で着手すればよく、上記の順序には組み込まない。

## 補足

- REQ-017/020は「不要」ではなく「判断材料（データ）待ち」。REQ-027は「データ蓄積待ち」ではなく「実売買がまだ存在しない」ことが理由。
